# Demo Assets

Run `python scripts/generate_sample_csv.py` to create `data/external/sample_transactions.csv` from the configured dataset. Place reviewed dashboard screenshots in `screenshots/` after local verification; generated screenshots are intentionally not committed by the application.
